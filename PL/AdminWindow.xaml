<!--<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="מסך ניהול ראשי" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            --><!-- אזור שעון --><!--
            <RowDefinition Height="Auto"/>
            --><!-- כפתורי קידום שעון --><!--
            <RowDefinition Height="Auto"/>
            --><!-- משתני תצורה --><!--
            <RowDefinition Height="*"/>
            --><!-- כפתורי פעולה --><!--
        </Grid.RowDefinitions>

        --><!-- אזור שעון המערכת -->
        <!-- Label לשעון המערכת --><!--
        <Label Grid.Row="0" FontWeight="Bold" FontSize="20" 
               Content="{Binding CurrentTime}" 
               HorizontalAlignment="Center" Margin="10"/>

        --><!-- כפתורי קידום שעון --><!--
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            --><!-- כפתור קידום דקה -->
            <!-- כפתור קידום שעה -->
            <!-- כפתור קידום יום -->
            <!-- כפתור קידום שנה --><!--
            <Button Content="Add One Minute" Click="btnAddOneMinute_Click" Margin="5"/>
            <Button Content="Add One Hour" Click="btnAddOneHour_Click" Margin="5"/>
            <Button Content="Add One Day" Click="btnAddOneDay_Click" Margin="5"/>
            <Button Content="Add One Year" Click="btnAddOneYear_Click" Margin="5"/>
        </StackPanel>

        --><!-- אזור משתני תצורה --><!--
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            --><!-- TextBox להצגת / עריכת משתנה תצורה --><!--
            <TextBox Width="100" Text="{Binding MaxYearRange, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            <Button Content="Update Max Year Range" Click="btnUpdateMaxYearRange_Click" Margin="5"/>
        </StackPanel>

        --><!-- כפתורי פעולה --><!--
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Open List Screen" Click="btnOpenListScreen_Click" Margin="5"/>
            <Button Content="Initialize DB" Click="btnInitializeDB_Click" Margin="5"/>
            <Button Content="Reset DB" Click="btnResetDB_Click" Margin="5"/>
        </StackPanel>

    </Grid>
</Window>-->
<Window x:Class="PL.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="מסך ניהול ראשי" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        FlowDirection="LeftToRight">

    <Grid Margin="20" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <!-- אזור שעון המערכת עם גבול -->
        <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1" Padding="10" CornerRadius="5" Margin="0,0,0,10" Background="#FFF0F0F0">
            <Label FontWeight="Bold" FontSize="24" 
                   Content="{Binding CurrentTime}" 
                   HorizontalAlignment="Center"/>
        </Border>

        <!-- כפתורי קידום שעון בתוך Grid עם מרווח אחיד -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="10" CornerRadius="5" Margin="0,0,0,10" Background="#FFF9F9F9">
            <UniformGrid Columns="4" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0">
                <Button Content="Add One Minute" Click="btnAddOneMinute_Click" Margin="5"/>
                <Button Content="Add One Hour" Click="btnAddOneHour_Click" Margin="5"/>
                <Button Content="Add One Day" Click="btnAddOneDay_Click" Margin="5"/>
                <Button Content="Add One Year" Click="btnAddOneYear_Click" Margin="5"/>
            </UniformGrid>
        </Border>

        <!-- אזור משתני תצורה בתוך StackPanel עם סידור יפה -->
        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="10" CornerRadius="5" Margin="0,0,0,10" Background="#FFF0F0F0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" >
                <Label Content="Max Year Range:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Width="100" Text="{Binding MaxYearRange, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                <Button Content="Update" Click="btnUpdateMaxYearRange_Click" Width="120"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" BorderBrush="Gray" BorderThickness="1" Padding="10" CornerRadius="5" Margin="0,0,0,10" Background="#FFF0F0F0">
            <StackPanel HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,20,0,5">
                    <TextBlock Text="Total Open Calls:" Width="120"/>
                    <TextBlock Text="{Binding TotalOpenCalls}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Total Closed Calls:" Width="120"/>
                    <TextBlock Text="{Binding TotalClosedCalls}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Total Expired Calls:" Width="120"/>
                    <TextBlock Text="{Binding TotalExpiredCalls}" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Total Open At Risk Calls:" Width="120"/>
                    <TextBlock Text="{Binding TotalOpenAtRiskCalls}" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Total In Progress Calls:" Width="120"/>
                    <TextBlock Text="{Binding TotalInProgressCalls}" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Total In Progress At Risk Calls:" Width="120"/>
                    <TextBlock Text="{Binding TotalInProgressAtRiskCalls}" VerticalAlignment="Center"/>
                </StackPanel>

            </StackPanel>
        </Border>
        
        <!-- כפתורי פעולה במרכז, עם ריווח יפה -->
        <Border Grid.Row="4" BorderBrush="Gray" BorderThickness="1" Padding="10" CornerRadius="5" Background="#FFF9F9F9">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" >
                <Button Content="Manage Volunteers" Click="btnOpenListScreen_Click" Margin="10" MinWidth="140"/>
                <Button Content="Manage Calls" Click="btnManageCalls_Click" Margin="10" MinWidth="140"/>

                <Button Content="Initialize DB" Click="btnInitializeDB_Click" Margin="10" MinWidth="120"/>
                <Button Content="Reset DB" Click="btnResetDB_Click" Margin="10" MinWidth="120"/>
            </StackPanel>
        </Border>

    </Grid>
</Window>
